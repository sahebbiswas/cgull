with open('cgull/rules/types_and_arrays.py', 'r') as f:
    content = f.read()

# Since I messed up the `self.create_issue` everywhere because I used `sed`-like replacements improperly earlier,
# The easiest way is to restore the file to the state before my edits.
