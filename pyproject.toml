[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.coverage.run]
source = ["cgull"]

[tool.coverage.report]
fail_under = 88.0
show_missing = true
