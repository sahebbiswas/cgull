# C-GULL Core Requirements
# AST parsing and preprocessing are runtime requirements. Security-relevant
# analyses (including offsetof/container recovery) rely on macro expansion;
# silently omitting these dependencies can reduce coverage.
pycparser>=2.21
pcpp>=1.30
